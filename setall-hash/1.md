### use time stamp:

- in hashmap, store key, value, timestamp(global count, increment at each step)
- when calling 'get', check elem, also check the timestamp, if timestamp < setall timestamp, return setted value
- if newer than setall, return original one
- so that the 'setall' is implemented with O(1) time
